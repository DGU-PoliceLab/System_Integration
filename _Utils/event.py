import cv2
from datetime import datetime
from _Utils.logger import get_logger
from variable import get_root_args

LOGGER = get_logger(name="[EVENT]", console=True, file=False)

def collect_evnet(target, pipe):
    pass